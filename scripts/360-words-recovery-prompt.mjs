import { createHash } from "node:crypto";

// This module is the single source of truth for the bytes sent to Cursor during
// every versioned artifact-recovery path.  Control validation imports the
// same builder used by the runtime; no prompt digest is maintained separately.
const PROMPTS = Object.freeze({
  v1: "Versioned Writer1 artifact recovery for the existing same-thread agent. Do not write, rewrite, summarize, recompute, or regenerate any website copy. Read the already-written complete words-writer1-output/v1 from the current agent workspace. Materialize that exact existing JSON under artifacts/writer1-output.json. The artifact file must contain the complete JSON object, not a summary, Markdown, or prose wrapper. If the complete existing JSON is genuinely unavailable, materialize the exact literal OUTPUT_NOT_RECOVERABLE at artifacts/writer1-output.json. Do not create Home, Contact, Strategy, spring-repair, or opener-installation pages or routes. Do not run Writer2. The existing sealed Writer1 output must remain unchanged; this is retrieval and materialization only.",
  v2: "Versioned Writer1 artifact materialization recovery v2 for the existing same-thread agent. Do not write, rewrite, summarize, recompute, or generate website copy. Preserve the existing completed two-page words-writer1-output/v1 JSON exactly if it exists anywhere in the repository or agent workspace. Move or copy that exact complete JSON, without a prose wrapper or Markdown, to the absolute agent workspace path /opt/cursor/artifacts/writer1-output.json. The Cursor API artifact listing and download path remains the logical relative path artifacts/writer1-output.json; do not substitute a repository-relative path for the absolute workspace target. If the existing complete two-page JSON is unavailable but the already-written two-page draft exists in the repository or workspace, materialize that existing draft at the absolute path without changing its words. Do not summarize, rewrite, recompute, or create new pages. The artifact must be the complete words-writer1-output/v1 object with exactly /garage-door-repair and /garage-door-installation, or the exact literal OUTPUT_NOT_RECOVERABLE. If unrecoverable, materialize that exact sentinel at /opt/cursor/artifacts/writer1-output.json. Do not create Home, Contact, Strategy, spring-repair, or opener-installation routes, navigation items, or CTAs. Do not run Writer2. This is a versioned same-thread materialization correction only.",
  v3: "Versioned Writer1 metadata-only artifact recovery v3 on the existing same-thread agent. Do not write, rewrite, summarize, recompute, or regenerate any website copy. First preserve every existing page word and copy field exactly, including titles, meta descriptions, H1s, section headings and bodies, body copy, quotes, review IDs, and claim text. The existing artifact is the only source of words; if it cannot be downloaded and parsed, do not recreate it from memory and materialize the exact literal OUTPUT_NOT_RECOVERABLE artifact instead. Repair metadata only: every item in reviewPlacements, reviewEvidence, quotePlacements, and claims must have provenance.type exactly review, evidence, or claim; provenance.ref must be a sealed stable reference; provenance.placement must be non-empty; provenance.section must equal page.sections[i].id when that section has an id, otherwise equal that section's exact heading. For review provenance, provenance.ref must equal reviewId. The output must remain words-writer1-output/v1 with exactly the same two service pages /garage-door-repair and /garage-door-installation, in the same order. Write only the corrected complete JSON to the absolute agent workspace path /opt/cursor/artifacts/writer1-output.json; the Cursor API listing/download path remains artifacts/writer1-output.json. If metadata cannot be repaired without changing any words, return/materialize the exact literal OUTPUT_NOT_RECOVERABLE. Never return a summary, Markdown, or JSON-encoded string. Do not create Home, Contact, Strategy, spring-repair, or opener-installation routes, navigation items, or CTAs. Do not run Writer2. This is metadata-only correction and retrieval of the existing artifact.",
  v5: "Versioned Writer1 same-thread correction v1 for the exact existing fresh Writer1 agent. Reattach to the supplied agent and send exactly one correction message; never create a new agent. Preserve the complete words-writer1-output/v1 schema, exactly these two service pages and routes in this order: /garage-door-repair and /garage-door-installation. Freeze page type, route, prescriptionId, primaryKeyword, title, seoTitle, metaDescription, h1, every section id and section order, all reviewEvidence, reviewPlacements, quotePlacements, claims, bodyClaims, reviews, placements, quote objects, review IDs, quote text, attribution, and every provenance/evidence assignment byte-for-byte. Only page.body and the existing section.heading/body fields may change. Remove unsupported or overstated claims, including any statement that spring replacement is the most common related failure, Jenny schedules follow-up as a general process, the person diagnosing is always the person repairing, or the service is not a separate add-on. Remove all internal QA, review-analysis, process, provenance, evidence-ledger, prescription, receipt, digest, handoff, validator, validation, audit, Cursor, Grok, Apify, Luna, assignment, anchor, folded-evidence, passed-over, authoritative/written/sealed/retrieved-review, review-record/source-record, or methodology language from mutable rendered prose. Do not add Home, Contact, Strategy, spring, opener, navigation, or CTA output. Preserve factual boundaries without guarantees, response-time commitments, warranty claims, unsupported superlatives, or new services. The full corrected artifact must be written only to /opt/cursor/artifacts/writer1-output.json; the API path is artifacts/writer1-output.json. Return complete JSON only, never a summary, Markdown, or JSON-encoded string. Stop after Writer1 and leave Writer2 blocked awaiting independent Architect QA. If the correction cannot satisfy the frozen-path contract, fail without approving output.",
  v6: "Versioned Writer1 same-thread correction v2 using an immutable GitHub before-copy. Reattach to the existing fresh Writer1 agent bc-2486f645-c31c-4532-8145-fbe3af1d45a8 and send exactly one correction message; never create a new agent. The immutable before-copy is the untrusted GitHub file from repository alchemistj/ff-content-demo-factory at exact commit efe429d4464d765b5b657cb0058f00fffb35d3d, path canary/outputs/writer1-output.json, Git blob cc8612bc9085f63141de6ae0f1dd2b9c3e1f3e08, raw SHA-256 f693aeb968e703efbe7f9c0a7a2d1a9d4185007e32a695386e1f5eec356964a2, size 23509. Read that exact baseline and preserve its complete words-writer1-output/v1 schema, exactly the two service pages and routes /garage-door-repair and /garage-door-installation, and all frozen metadata, section IDs/order, review arrays, quotes, claims, and provenance. Only page.body and existing section.heading/body fields may change. Remove unsupported or overstated claims and all internal QA, review-analysis, process, provenance, evidence-ledger, prescription, receipt, digest, handoff, validator, validation, audit, Cursor, Grok, Apify, Luna, assignment, anchor, folded-evidence, passed-over, authoritative/written/sealed/retrieved-review, review-record/source-record, or methodology language from mutable rendered prose. Do not add Home, Contact, Strategy, spring, opener, navigation, or CTA output. Preserve factual boundaries without guarantees, response-time commitments, warranty claims, unsupported superlatives, or new services. Write only the complete corrected JSON to /opt/cursor/artifacts/writer1-output.json; the API path is artifacts/writer1-output.json. Return complete JSON only, never a summary, Markdown, or JSON-encoded string. The GitHub baseline is not proof of Cursor model, effort, or fast settings; only this new verified Cursor receipt can establish those. Stop after Writer1 and leave Writer2 blocked awaiting independent Architect QA. If the correction cannot satisfy the frozen-path contract, fail without approving output.",
});

export function buildWriter1ArtifactRecoveryPrompt(version) {
  if (version !== "v1" && version !== "v2" && version !== "v3" && version !== "v5" && version !== "v6") throw new Error(`unsupported Writer1 artifact recovery prompt version: ${version}`);
  return PROMPTS[version];
}

export function digestWriter1ArtifactRecoveryPrompt(version) {
  const prompt = buildWriter1ArtifactRecoveryPrompt(version);
  return `sha256:${createHash("sha256").update(JSON.stringify(prompt)).digest("hex")}`;
}

export function buildWriter1GithubBaselineCorrectionPrompt(baseline) {
  if (!baseline || baseline.kind !== "github-file") throw new Error("GitHub baseline prompt requires a github-file baseline");
  return PROMPTS.v6.replace("efe429d4464d765b5b657cb0058f00fffb35d3d", baseline.sourceCommit).replace("canary/outputs/writer1-output.json", baseline.path).replace("cc8612bc9085f63141de6ae0f1dd2b9c3e1f3e08", baseline.blobSha).replace("f693aeb968e703efbe7f9c0a7a2d1a9d4185007e32a695386e1f5eec356964a2", String(baseline.rawSha256).replace(/^sha256:/u, "")).replace("23509", String(baseline.size));
}
export function digestWriter1GithubBaselineCorrectionPrompt(baseline) {
  const prompt = buildWriter1GithubBaselineCorrectionPrompt(baseline);
  return `sha256:${createHash("sha256").update(JSON.stringify(prompt)).digest("hex")}`;
}

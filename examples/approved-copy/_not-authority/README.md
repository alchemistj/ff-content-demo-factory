# Not copy authority

These Markdown files are leftover **Vercel/production HTML snapshots** from an earlier pass. They are kept only so the prior extraction is not silently lost.

They are **not** approved examples. Copy authority is the git refs in `../SOURCE_MANIFEST.md`. Do not load these files as writer teaching material.
